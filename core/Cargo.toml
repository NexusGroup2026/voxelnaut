[package]
name = "core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
parking_lot = "0.12"
lazy_static = "1.4"
slotmap = "1.0"
noise = "0.9"
chrono = "0.4"
thiserror = "1.0"
toml = "0.8"

[features]
default = []