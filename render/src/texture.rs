//! Texture module placeholder
//! Textures are handled via wgpu texture views

/// Texture manager
pub struct TextureManager;

impl TextureManager {
    pub fn new() -> Self {
        Self
    }
}

impl Default for TextureManager {
    fn default() -> Self {
        Self::new()
    }
}